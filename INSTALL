# these are instructions for starting inititing an SQLite based system
# for MySQL based one you need to adjust the commands slightly

perl Makefile.PL
(sudo) make installdeps

# create a local config file, suchs as nblog_local.pl with your database connect info
cp nblog.pl nblog_local.pl

script/create_sql.pl 

# import one of the generated SQL schemas.
sqlite3 nblog.db < Nblog-Schema-1.x-SQLite.sql

# import sql/nblog_initial_data.sql
sqlite3 nblog.db < sql/nblog_initial_data.sql 

script/create_login.pl
script/nblog_server.pl

# login into http://localhost:3000/

